site_name: arxiv2025_mascs_gen
nav:
  - Home: index.md

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      project_name: arxiv2025_mascs_gen
      handlers:
        python:
          options:
            docstring_style: numpy